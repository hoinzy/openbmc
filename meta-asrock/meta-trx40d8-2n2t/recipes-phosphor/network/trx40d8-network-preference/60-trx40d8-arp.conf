# Both management interfaces can be attached to the same broadcast domain.
# Answer ARP only on the interface that owns the requested address so the
# dedicated and NCSI addresses cannot overwrite each other's neighbor entry.
net.ipv4.conf.all.arp_ignore = 1
net.ipv4.conf.all.arp_announce = 2
